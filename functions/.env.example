SMTP_HOST=mail.1pwrafrica.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=notifications@1pwrafrica.com
SMTP_PASSWORD=your_smtp_password_here
APP_URL=https://pr-system-1pwr.web.app

# HR employee metadata sync (HR portal is canonical source of truth).
# Server-to-server only — never expose HR_API_KEY_PR_PORTAL to the client.
HR_API_BASE_URL=https://hr.1pwrafrica.com
HR_API_KEY_PR_PORTAL=your_hr_api_key_here

# Fleet Hub mission client (field-camp provisioning prepopulation).
# Server-to-server only — never expose FLEET_INTEGRATION_API_KEY to the client.
FLEET_API_BASE_URL=https://fm.1pwrafrica.com
FLEET_INTEGRATION_API_KEY=your_fleet_integration_key_here
